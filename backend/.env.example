# Backend Environment Variables Example
# 
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a .env
# 2. Modifica los valores según tu entorno (local o Docker)
# 3. NUNCA commitees el archivo .env (contiene secretos)
#
# Archivo .env.example está en .gitignore para evitar exponer credenciales

# ========== Server Configuration ==========
# Puerto en el que escucha el backend
PORT=3000

# ========== Database Connection ==========
# 
# OPCIÓN A: Ejecutando en DOCKER COMPOSE (recomendado)
# Usa el nombre del servicio 'db' definido en docker-compose.yml
DATABASE_URL=postgresql://postgres:[PASSWORD]@db:5432/task_nginx

# OPCIÓN B: Ejecutando LOCALMENTE contra Postgres local
# Descomenta esta línea si corres el backend en tu máquina (sin Docker)
# DATABASE_URL=postgresql://postgres:PASSWORD@localhost:5432/task_nginx

# ========== Postgres Connection Parameters ==========
# (Usados como alternativa a DATABASE_URL en algunas configuraciones)
PGHOST=db
PGPORT=5432
PGUSER=postgres
PGPASSWORD=[PASSWORD]
PGDATABASE=task_nginx

# ========== Node Environment ==========
# Opciones: development, production, test
NODE_ENV=development
